# Tarefas — EIXO V2

## Pendente de deploy

1. Fase 2/7 — Substituir tabela de fazendas por cards em `Farms.tsx` (capacidade total UA, animais atuais, % de área distribuída e expansão de pastos).
2. Fase 2/8 — Exibir resumo de capacidade total estimada (UA) antes dos botões finais em `FarmRegistrationForm.tsx`.
3. Fase 2/9 — Desdobrar forrageiras em cultivares com `<optgroup>` no `FarmRegistrationForm.tsx` (Brachiaria, Panicum e novas opções).
4. Fase 2/10 — Mostrar nome da fazenda no stepper do passo 2 em `FarmRegistrationForm.tsx`.
